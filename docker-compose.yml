version: '3'
services:
  web:
    build: .
    ports:
      - "5000:80"
    environment:
      - PORT=80
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge